# Übung 3 – Einen aktiven Change archivieren

## Kontext

Der Change `us-06-dashboard` ist vollständig implementiert.

## Ziel

Archiviert den Change und schaut euch danach an, was sich in `openspec/` verändert hat.

## Erfolgskriterium

`openspec list` zeigt keine aktiven Changes mehr. Die Specs aus `us-06-dashboard` sind in `openspec/specs/` eingeflossen.
